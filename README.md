#-----------------------------------
#   DESCRIPTION
#-----------------------------------

It is a CBOW --> It is trained to see what are the words that come together. Is to fill the missin word. 
You can also make semantic calculations on similarity. 

#-----------------------------------
#   CONTENTS
#-----------------------------------
- DL script to train the algorithm and save the model.
- a stramlit app to load the algorithm and use it to see simlarity of words do the semantic calculations.

#-----------------------------------
#   Environement on MacOs
#-----------------------------------
- Conda +tensorflow in MacOS is not compatible. --> must do it with pyenv and a version of tensorflow + python 3.10

- 
